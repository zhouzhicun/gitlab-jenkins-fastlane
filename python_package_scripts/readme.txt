
运行环境：python2.7.10

依赖于：biplist(用于解析plist文件), exchangelib(用于发送邮件)

biplist安装命令:
sudo easy_install biplist
sudo easy_install exchangelib




脚本配置:

PackageConfig.plist  用于配置打包时的相关信息，例如git仓库， svn地址，工程相关配置等。

AccountConfig.py 用于配置svn账号，和邮箱地址等信息

